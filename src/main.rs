//use std::fs;
//use executor::ConcolicExecutor;
//use external::pcode_parser::Parser; 

fn main() {
//    let ctx = Context::new(&z3::Config::new());
//    let mut state = State::new();

//    let pcode_data = fs::read_to_string("tests/pcode_data1.txt").expect("Failed to read Pcode file");
//    let parsed_instructions = Parser::parse(&pcode_data).expect("Failed to parse Pcode");

//    let mut executor = ConcolicExecutor::new();
//    executor.run(parsed_instructions);
}
